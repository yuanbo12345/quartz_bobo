### AV检测一般分成三类
静态检测，动态检测，启发式或者行为式检测

### 静态检测
静态检测技术是最简单的防病毒检测类型，它基于恶意文件的预定义签名。简而言之，它在检测中使用模式匹配技术，例如查找唯一字符串、CRC（校验和）、字节码/十六进制值序列和加密哈希（MD5、SHA1 等）。
然后，它在操作系统内的现有文件与签名数据库之间执行一组比较。如果数据库中存在该签名，则认为该签名是恶意的。

### 绕过静态检测的方法

