# 课堂随记
1. 卡巴斯基的静态检测对内存属性函数非常敏感，内存属性函数出现`VirtualAlloc.Call(0, uintptr(len(shellcode)), 0x1000|0x2000, 0x04);`尤其是`0x1000|0x2000`两个参数同时存在，可以只保留`0x2000`尝试一下。


# 课堂疑问
1. 在大白哥的课程中，利用go作为语言编写常规loader即内存申请和内存复制函数还有内存属性以及最后的shellcode内存执行函数都是常见的API，在修改了内存申请属性之后，静态直接过掉，而且上线也没报毒，难道卡巴斯基不会杀loader中的关键常规的api字符串，也不会对常规api进行hook吗？（<mark style="background: #FF5582A6;">重要问题</mark>）
2. 在1中的loader编写思路情况之下，木马成功上线，但是执行whoami之后立刻显示行为有问题被查杀，那是不是说明卡巴的行为检测只针对木马上线之后的后渗透阶段？
3. 
4. 