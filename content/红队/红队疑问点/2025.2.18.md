### 疑问一
1---在timline中学的免杀是一种shellcode加载器，shellcode是通过cs生成的，可不可以利用lnk文件使用ps脚本下载time中学的加载器文件，并利用ps脚本执行的方式免杀在不借助社会工程学的方式下下载执行shellcode？

2---timeline中的shellcode加载器最后的代码是什么格式的文件，dll还是exe？

3---现代apt组织都是利用脚本下载执行各种嵌套的恶意文件之后才然远控上线，并没有先使用shellcode在利用shell上线远控，这两种方法有什么优缺点？

4---如果我想利用timeline中学到的加载器方式直接加载一个开源的远控，不加载shllcode了，那么加载器代码应该怎么改，开源的远控应该改造那些部分实现免杀，应该怎么把二者结合起来？
timeline教授的shellcode加载器的编写中将CSRAT远控变成了shellcode二进制文件放到加载器文件中执行，那如果使用其他开源的RAT远控是否也可以将其转换成shellcode放到加载器中执行？

5---现代AV软件对ps脚本的执行检测严格程度怎么样，最严的检测是检测哪里？

6---自己在按照apt组织常用的初始访问方式学习ps脚本时发现根本无法形成自己的思路，感觉东西太多了，应该怎么学，学什么地方，有必要按照apt的进攻方式去学习吗？

7---如果dll文件中只有函数和方法，那apt组织是怎么通过加载dll文件释放其他格式的文件（文档文件，exe，dll），dll不是代码那怎么被系统执行恶意攻击的呢？

8---怎么让lnk文件释放一个诱饵文件落地指定路径，释放的恶意文件落地内存，两个文件均被自动执行？

9---