## 库的安装

先找到py环境目录--在IDE的命令行中
```bash
python -c "import sys; print(sys.executable)"
```
或者在cmd中执行
```bash
where python //windows环境下
```

```bash
which python //linux环境下
```

<mark style="background: #FF5582A6;">对于py来讲可以直接使用pip命令安装库，pip工具会自动将库安装到py环境包中</mark>
