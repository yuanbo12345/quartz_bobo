1---Win32 API，一个用于用户模式应用程序和内核之间接口的库,可以为应用程序修改windows子系统和内核作为中间桥梁

2---Windows 通过两种不同的模式来区分硬件访问：用户模式和内核模式。这些模式决定了应用程序或驱动程序允许的硬件、内核和内存访问。各模式之间的API或系统调用接口，向系统发送信息以在内核模式中进行处理。

![[image-3.png]]

win API就是为了给两种模式的转换提供入口和方法。下面是用户应用程序如何使用 API 调用来修改内核组件的直观表示。

![[image-4.png]]



