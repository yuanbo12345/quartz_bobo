```matlab
% 添加dis面积差的调试来验证是否为逐个像素计算面积差
            % 让用户选择保存位置
            [filename, pathname] = uiputfile('*.xlsx', '保存标定结果到Excel');
            if isequal(filename, 0) || isequal(pathname, 0)
                   return; % 用户取消了保存
            end

            % 构建完整文件路径
            fullpath = fullfile(pathname, filename);

            % 保存dis2Area到Excel (假设dis2Area是您要保存的变量)
            writematrix(app.dis2Area, fullpath);
            % 注意此时的app.dis2Area是调用的app的全局属性变量，如果是局部变量只能是dis2Area不能有app.
            
            % 显示成功消息
            msgbox(sprintf('数据已保存到:\n%s', fullpath), '保存成功', 'info');
```

- 此调试代码的功能就是自动将变量值保存到选中的文件夹中
- 注意全局属性和局部属性的调用，所有函数之间通用的是全局属性需要加`app.`局部属性不需要加