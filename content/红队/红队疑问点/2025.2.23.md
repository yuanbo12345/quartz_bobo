1--木马起作用之后建立的代理隧道（利用tcp和socks5）和初始访问阶段中cs软件的beacon利用https协议通信有什么不一样吗？那安全性如何，不能在现阶段就使用tcp流量通信吗？

2--beacon是什么？可以和毒液代理隧道软件互联

3--管道是什么意思？

4--time课程中关于代理隧道的部分最后目标主机和c2以及c2和攻击机之间的所有通信流量都是socks5的吗，为什么一开始建立的时候就不能直接使用socks5而要先使用tcp连接之后再使用socks5呢？

5--cs软件中可以使用socks5代理流量吗？在time中使用毒液的多级代理功能时，好像只是对节点2（不出网的内网节点）开了一个只运行socks流量的端口，可能是由于这台主机不出网所以原先端口全都被关闭，现在需要手动打开一下。问题：为什么没对服务器和节点1的主机也各开一个socks协议的端口？（**重点问题**）

6--从攻击机到服务器再到多节点目标主机，来回的通信过程到底是什么样的，都是使用什么协议进行通信的？

7--重点研究CS软件的代理通信配置功能？