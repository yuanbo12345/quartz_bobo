# EDR规避技术随记
1. 当我的马上线之后，edr对我后渗透阶段的检测，是不是主要通过内存扫描看线程栈以及进程调用链是否触发规则？
2. edr的API Hook一般进行的是什么方面的检测？只是当hook的函数如果被调用之后，edr只是分析传入该函数的参数吗？还是分析什么？
3. TSL/SSL的具体技术原理和通信过程？
4. 晚上说现在后渗透阶段很多是通过上线建立起socket5代理之后，通过隧道进行通信，这个和与上线马（实质是beacon.dll）之间通信有什么优缺点，在规避edr的查杀上？是不是这种方法也仅限于进行命令执行，其他操作也不可以？
5. 了解一下socket5代理进行后渗透的技术原理？
6. 所以说全部模仿合法应用程序的调用方式，加载方式，通信方式隐蔽才是实质？
7. 如果在我们的冲锋马安全上线之后，我通过将开源的后渗透软件（例如frp）还在成bof的形式，在马上执行该bof，那么内存中的线程调用堆栈会是什么样？
8. 对于一些内核态的恶意软件操作edr又是怎么进行查杀的？
9. VMProtect又是什么意思，技术原理是什么？

# 高级APT以及勒索软件对EDR的规避手法
1. 总结：大部分都是通过带有安全签名的反Rootkit软件去禁用edr之后，再进行后渗透
https://lumu.io/blog/cisa-reveals-ransomware-gangs-bypassing-edrs/#elementor-toc__heading-anchor-1


# 2025.8.11学习随记
# 合法线程堆栈欺骗技术
## Bof对线程堆栈调用的影响
### 疑问
1. 合法堆栈的调用示意图怎么看？主要是调用顺序，还有那个windbg上调用栈怎么看？
2. 线程的调用堆栈会显示代码中自定义的函数吗？这些自定义的函数在调用栈中没法被解析会以什么名字显示出来？
3. 我记得调用R3层的API函数会被一层层的解析直到调用到R0层功能相同的驱动，这段调用不是用户写入代码中的吧？，如果用户写的代码中需要调用多个驱动功能代码（R0层函数）那么从R3的嗲用到R0的调用会怎么在堆栈上展示出来，这段调用过程可以通过windbg类的软件查看吗？这段调用过程和内存线程调用栈之间有什么关系吗？能查看到内核层的具体调用函数吗，具体怎么操作？
4. 写loader的时候，会在写很多用户层的功能性API函数的调用，这些api函数的功能实现不是要靠从用户层都内核层的api函数连续调用直到驱动函数去实现吗，那么这个过程会展现到线程调用堆栈中吗？一个loader中会有很多这种用户层的api调用，那么每个用户层api功能的实现也就是从R0到R3的调用都会显示到线程堆栈中吗？
5. beacon.dll的技术原理？为什么beacon.dll不进行二开会被内存扫描发现特征，当我们调用CS或者使用BOF的时候beacon.dll会起什么作用？


# 1. 先把脚本下载到本地
curl -fsSL https://raw.githubusercontent.com/SuperManito/LinuxMirrors/main/ChangeMirrors.sh -o /tmp/ChangeMirrors.sh

# 2. 再运行（可不加 sudo，脚本内部会自己提权）
bash /tmp/ChangeMirrors.sh --edu


sudo tee /etc/apt/sources.list >/dev/null <<'EOF'
deb http://archive.ubuntu.com/ubuntu/ noble main restricted universe multiverse
deb http://archive.ubuntu.com/ubuntu/ noble-updates main restricted universe multiverse
deb http://archive.ubuntu.com/ubuntu/ noble-security main restricted universe multiverse
deb http://archive.ubuntu.com/ubuntu/ noble-backports main restricted universe multiverse


我想实现就是我一共一个命令，然后大模型根据我的命令分析出关键点，然后根据关键点自动查找相关的文献，然后把契合度高的文献自动下载到本地之后，再从文献里面提取我需要的关键内容信息整合到一起反馈给我，给我推荐能完美实现这一套流程的开源项目或者工具，最好可以利用cursor中自带的大模型或者可以利用geminicli实现，如果实在不能用一个工具实现可以使用多个工具，要求最后总结的关键点信息一定是准确很契合我要求的


gemini prompt "请把这个文件夹中的所有文献一个个的进行提取关键内容，提取文献中关于微型燃气轮机仿真的具体步骤以及三维模型建立以及燃烧性能采用的研究方法内容，将所有文献的这些关键内容整合到一起，要包含文献名称和我要求的管家内容" --files ./E:\Trafomerlearn\仿真文献/*.pdf --md > final.md 


1. 刚才报道流程的一些信息，可能网络有点波动，你能再介绍一下吗
2. 学校的平面图能给我一份吗

@Machine\ learning\ based\ spray\ process\ quantification-mono.pdf 请仔细阅读这个篇文档，然后将文献的大体实验结构和步骤整理出来，同时将文献中具体采用了什么机器学习算法，以及为什么采用该算法和后续机器学习的具体实验过程整理出来，以及怎么通过机器学习衡量喷雾量化的原理也整理出来，按照我的要去一定要详细将文献中的内容全部提取出来，一定要能详细回答我的所有问题，最后都放到一个markdown格式的文献综述文件中，下载到本地即可

仔细阅读这篇文献之后，把这篇文献中所采用的算法的结构，算法在该研究中的原理详细介绍一下，然后就是把这个论文中改算法的缺点和不足以及为什么会产生这个缺点也一定要介绍全面了，最后针对这个研究课题主流的研究解决方案都是什么，优缺点分别是啥，以及采用机器学习方法量化喷雾液滴参数的难度和缺陷在什么地方，为什么有这种缺陷。这些都要全部详细讲解明白，如果论文中没有提到就收集现在该研究方向的权威真实信息，务必要求完全按照我的要求 ，详细的一一回答全面，搜索的延展信息要真实有效。最后以markdown格式扩写到

帮我推荐一个公开的数据集，要求格式是.csv的，表格类数据集，有数字也有字符串，列数大约10列左右，数据量1万左右，可以直接下载csv格式数据集到本地的，直接告诉我符合我数据集要求的下载网址就可以

详细解释一下使用的每个函数的作用，参数，图片中的意义


这是单词本的.json格式的图片展示，根据图片中前几段代码的形式，给我把这个项目涉及到的地方重新修改完善，你把需要单词本路径的地方空出来就行，告诉我怎么改，我自己把路径放上去 


我现在有个研究课题：我有喷雾液滴的阴影图片上万张，现在我需要的就是构建一种生成框架，输入工况以及极限工况下就可以根据这个工况条件生成喷雾之后的液滴的具体图像，或者说相关的液滴掩码也可以，这属于深度学习方面的什么领域的研究，现在主流的研究中这方面采用的解决方案都有什么，详细分析每个方案的优缺点，给我一个精度比较高的方案推荐，给我写成一份markdown格式的汇报文档，要求所有信息必须真实，然后下载到本地 

我想要在项目中实现，将我的字典json文件单独放置到一边，js需要的时候调用，不要把字典放到js里面，然后项目只采用前端结构，不涉及后端和数据库的搭建即可没，字典文件是


这是项目现在的问题，我想要在项目中实现将我的字典json文件单独放置到一边，js需要的时候调用，不要把字典放到js里面，然后项目只采用前端结构,不涉及后端和数据库的搭建即可没，字典文件的格式是这个图片显示的样式，你现在根据这个样式给我优化代码，然后我可以自己在index代码中输入json字典路径就可以


这是我大致做的课题，主要是输入各种工况条件，尤其是极限情况下的工况条件，然后生成符合真实物理过程的喷雾液滴图像，我有上万张真实实验情况下的喷雾液滴图像，我的课题重要研究标砖就是模型根据各种工况生成的掩码一定是精度准度比较高，多物理因素的耦合作用下的液滴掩码也要十分精确，根据现在相关的研究前沿，给我设计精度比较高，兼容工况比较强的模型方案

这是我的项目文件，问题是现在显示的单词都是无法加载字典文件而使用的默认单词，我项目架构是把字典文件单独放到一个json文件中，然后js文件需要的时候调用，但是失效了，不给你字典文件了，你给我项目中写一个字典文件的路径，到时候我自己填入就可以，你给我解决问题


